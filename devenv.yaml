# yaml-language-server: $schema=https://devenv.sh/devenv.schema.json
inputs:
  nixpkgs:
    url: github:cachix/devenv-nixpkgs/rolling
  treefmt-nix:
    url: github:numtide/treefmt-nix
    flake: false
  devenv-base:
    url: github:otahontas/devenv-base?ref=1.0.2
    flake: false

imports:
  - devenv-base
